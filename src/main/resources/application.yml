spring:
  application:
    name: pick-place-api
  config:
    import:
      - classpath:/prop/file.yml
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/pickplace?characterEncoding=UTF-8&allowMultiQueries=true
    username: pickplace
    password: pickplace
  jpa:
    hibernate:
      ddl-auto: none # TODO - 항상 none 인지 확인
